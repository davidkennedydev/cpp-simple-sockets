version: 2
jobs:
  build:
    machine:
      image: circleci/classic:latest
    steps:
      - checkout
      - run: docker build -t DavidUser/cpp-simple-socket
      - run: docker run --rm DavidUser/cpp-simple-socket
