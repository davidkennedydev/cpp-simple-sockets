#!/bin/sh

apt-get update 

# Install build tools
apt-get install -y \
	clang++-3.8 \
	g++ \
	make; \
	cd /usr/bin && ln -s clang++-3.8 clang++ # Configure clang++ alias

# Install dependencies
apt-get install -y \
	libstdc++6 \
	libpthread-stubs0-dev \
	libboost-dev
